# Production Release UAT — Build 12.0.0

Use `artifacts/build-12.0.0/production-release-uat-workbook.csv`.

Execute all forty-eight scenarios against isolated production-equivalent staging using the exact release commit. Evidence must include screenshots or recordings, request/response identifiers, database rows, audit events, role and tenant results, performance output, backup/restore/rollback records, sign-offs, and the defect register.

A synthetic fixture result cannot replace deployed evidence. Any P0/P1 defect or HOLD decision blocks production authorization.
