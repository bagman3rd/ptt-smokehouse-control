# Test Report — Build 6.8.0

Static validation covers the new session schema and migration, production privileged-2FA guard, session revocation actions, multi-store rollup, 200-request load configuration, version consistency, migration compatibility aliases, and flat ZIP packaging.

The full frozen dependency install, Prisma generation, TypeScript compilation, migration replay, production Next.js build, Playwright suite, restore drill, and load smoke are delegated to GitHub Actions because this sandbox could not reach the npm registry.
