# Test Report — Build 6.4.0

Static release checks verify the committed lockfile, removal of the bootstrap workflow, strict CI lockfile enforcement, sanitized health output, deployed-build verification, authenticated smoke credentials, hardened cross-tenant 404 assertion, and clean deployable artifact structure. Full dependency installation, compilation, migrations, and browser tests run in GitHub Actions with Node 20 and PostgreSQL 16.
