{
  "name": "ptt-smokehouse-control",
  "version": "1.2.2",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start -p ${PORT:-3000}",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "render-build": "npm install --legacy-peer-deps && npx prisma generate && npx prisma migrate deploy && npm run build"
  },
  "dependencies": {
    "@prisma/client": "^6.11.1",
    "next": "^14.2.30",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "prisma": "^6.11.1"
  },
  "engines": {
    "node": ">=20 <23"
  }
}
