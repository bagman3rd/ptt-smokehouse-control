import assert from 'node:assert/strict';
import { readFileSync } from 'node:fs';
const read=(p)=>readFileSync(p,'utf8');
const pkg=JSON.parse(read('package.json'));
assert.equal(pkg.version,'7.9.0');
assert.ok(read('app/api/cook-plan/route.ts').includes("status: 400"));
assert.ok(read('lib/tenantGuard.ts').includes('tenantGuardEnabled'));
assert.ok(read('render.yaml').includes('TOTP_ENCRYPTION_KEY'));
assert.ok(read('package.json').includes('test:e2e:concurrency'));
console.log('Build 7.9.0 comprehensive acceptance checks passed.');
