# Test Report — Build 9.5.0

Static release checks verify package/build metadata, POS migration idempotency, the legacy-schema replay harness, Archer demo-mode gating, navigation/page parity, and the existing EOD/POS/chatbot contracts. The PostgreSQL upgrade replay is wired into CI and requires the CI PostgreSQL service.
